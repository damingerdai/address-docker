{
  "name": "class-pass",
  "version": "0.0.2",
  "description": "a simple class system",
  "main": "index.js",
  "scripts": {
    "init:project": "sh ./scripts/init.sh",
    "start": "docker-compose up",
    "changelog": "npx conventional-changelog -p angular -i CHANGELOG.md -s",
    "changelog:all": "npx conventional-changelog -p angular -i CHANGELOG.md -s -r 0",
    "version": "yarn changelog:all && git add CHANGELOG.md"
  },
  "keywords": [
    "docker",
    "docker-compose",
    "nodejs"
  ],
  "author": "arthur ming",
  "license": "ISC",
  "devDependencies": {
    "conventional-changelog-cli": "^2.0.23"
  }
}
